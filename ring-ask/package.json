{
  "name": "ring-ask",
  "version": "0.4.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc --noEmit && vite build",
    "test": "node --test tests/*.test.mjs",
    "pack": "npm run build && evenhub pack app.json dist --sdk-ver 0.0.15 -o ring-ask.ehpk"
  },
  "dependencies": {
    "@evenrealities/even_hub_sdk": "0.0.15",
    "@evenrealities/pretext": "0.1.4"
  },
  "devDependencies": {
    "@evenrealities/evenhub-cli": "0.1.14",
    "typescript": "latest",
    "vite": "latest"
  }
}
